[tool.uv.workspace]
members = ["sdk", "cli"]

[dependency-groups]
dev = [
    "commitizen==4.16.2",
    "hatch==1.16.5",
    "httpx==0.28.1",
    "pyright==1.1.409",
    "pytest==9.0.3",
    "pytest-asyncio==1.3.0",
    "ruff==0.15.14",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.uv]
package = false

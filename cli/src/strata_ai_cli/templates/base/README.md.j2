# {{ project_name_title }}
Generated by `uvx strata-ai-cli create-agent`. Pattern: `{{ pattern }}`

## 🚀 Quick Start
```bash
cp .env.example .env && uv sync
uv run dev  # hot-reload :8080, console OTel, mock AI
uv run test # deterministic, zero LLM calls